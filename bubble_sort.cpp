// Falis Gaal 
// Project 1 
// July 6th 
/* this Program creates a linlked list from user input.
 displays it, sorts it using bubble. sort and then shows 
 the sorted result. memory is probably cleaned up*/
 
#include <iostream>
using namespace std;
  
struct node {
    int data;     // Stores the actual value
    Node* next;  // Pointer to next node (or null if end)

};
